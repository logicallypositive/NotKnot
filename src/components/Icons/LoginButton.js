import { Button } from "@mui/material";

export default function LoginButton() {
  return (
    <Button
      style={{
        color: "white",
        fontSize: "1rem",
        fontWeight: "bold",
        textTransform: "none",
      }}
    >
      LOGIN
    </Button>
  );
}
